---
import Default from "@astrojs/starlight/components/PageTitle.astro";

const isBare = Astro.locals.starlightRoute.entry.data.bare;
---

{
  !isBare && (
    <Default {...Astro.props}>
      <slot />
    </Default>
  )
}
